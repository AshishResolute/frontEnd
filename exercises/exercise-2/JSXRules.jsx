export const JSxXRules = ()=>{
    return (
        <div>
            <h1>JSX Rules</h1>
            <p>JSX Component can just return a single parent element as a function can return a single element</p>
            <p>JSX elements must be properly closed</p>
            <p>JSX properties must be written in camelCase as className,htmlFor</p>
        </div>
    )
}